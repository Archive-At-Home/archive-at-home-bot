services:
  bot:
    image: ghcr.io/archive-at-home/archive-at-home-bot:latest
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config/config.yaml:ro
      - bot_data:/app/data

volumes:
  bot_data: